"""QR Text Reader — small, offline, secure QR-to-raw-text tool.

This package intentionally does NOT parse, decode further, or interpret
QR contents in any way. It only extracts the raw string payload of a QR
code and copies it to the clipboard verbatim.
"""

__version__ = "0.1.0"
